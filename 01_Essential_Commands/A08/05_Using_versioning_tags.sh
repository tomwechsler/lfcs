#Working on ubuntu1

#Change the directory
cd project1

#The status
git status

#Add a tag
git tag v1.0

#The status
git status

#Git show
git show

#Git push
git push origin v1.0

#Show the tags
git tag

#Git show
git show v1.0

#Change to home directory
cd ~

#Remove the project1 directory
rm -rf project1

#Clone the git repository with the tag
git clone -b v1.0 git@localhost:/home/git/git/project1

#Change the directory
cd project1

#Show the files
ls -la

#Show the content of the file
cat hello.sh

#The git status
git status

#Change to home directory
cd ~

#Remove the project1 directory
rm -rf project1

#Clone the git repository with the tag
git clone git@localhost:/home/git/git/project1

#Change the directory
cd project1

#The git status
git status
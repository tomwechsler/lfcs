#Working on ubuntu1

#List the git config
git config --list

#The global git config
git config --global --list

#List all files
ls -a

#Configure the git global settings
git config --global user.name "Tom Wechsler"
git config --global user.email "tom@example.com"

#The global git config
git config --global --list

#The repo git config
git config --list